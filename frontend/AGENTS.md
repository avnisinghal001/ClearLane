# AGENTS.md — Frontend (`frontend/`)

> Scope: the React command-center dashboard. Read the root `AGENTS.md` first — the
> honesty contract applies to all UI copy, and the three-number separation governs
> how operational state is displayed.

## Stack

- **React 18 + Vite 5**, plain JSX (no TypeScript, no router lib — hash-based routing).
- **react-leaflet / leaflet** for maps, **recharts** for charts, **qrcode.react**
  for local QR (no remote QR service).
- No CSS framework — a single hand-written stylesheet at `src/styles/app.css`.

```bash
cd frontend && npm install
cp .env.example .env          # VITE_API_BASE empty -> same-origin /api (Vite proxy in dev, Vercel fn in prod)
npm run dev                   # http://localhost:5173
npm run build                 # -> dist/  (bundle is ~150KB gz; keep it lean)
```

## The offline-first contract (do not break this)

**The dashboard must always render — even with no backend and no internet.** This
is a judging-safety requirement.

- `src/lib/api.js` is the single API layer. Every read call tries
  `VITE_API_BASE + path`, and on any failure falls back to a bundled
  `public/demo/*.json` file. When a call fails it flips `LIVE = false` and the
  header badge shows "DEMO (offline)".
- The operational write loop (`opSnapshot`, `opComplaint`, `opDispatch`,
  `opFeedback`, `opPatchStatus`) tries the backend, then falls back to
  `src/lib/localOps.js` — an **in-memory mirror of `backend/app/operational.py`'s
  exact rules** (OP_RULES, BBOX, nearest-zone-600m, decay, clamp). If you change
  those rules on the backend, change `localOps.js` to match.
- The demo bundle in `public/demo/` is regenerated by the ML pipeline's
  `run_all.py` (do not hand-edit it). If you add an artifact the UI needs, make
  the pipeline emit + bundle it, add a backend route, and add a demo fallback in
  `api.js`.

## App shell & routing (`src/App.jsx`)

- State lives in `App`: `payload` (map_payload), `snapshot` (operational layer),
  `view`, `selected` zone, `filter`, `flyTo`, mobile flag.
- It loads `/api/map/payload` once, seeds the offline zone index
  (`seedOpZones`), and **polls the operational snapshot every 5s** so the command
  center visibly updates.
- Routing is **hash-based**:
  - `#/dispatch/{id}` → standalone mobile `Dispatch` screen;
  - `#/officer` (or any phone-width screen unless `#/dashboard`) → `OfficerView`;
  - otherwise the full dashboard with the nav `VIEWS`.
- `VIEWS` = Command Map, Today / Emergency, Priority Queue, Flow Impact, Staffing,
  Repeat Offenders, Operations Loop, Timing Gap, Coverage/ROI, Forecast, Typology,
  Station Command, Methodology & Validation.
- A **global Time Lens** (`lens` state + `daily` series in `App`, `lib/timeLens.js`,
  `TimeLensBar.jsx`) re-weights the map, Priority Queue, Flow Impact and Staffing to
  a chosen **calendar date** (or range). A date inside the data window shows
  recorded activity; a future date (e.g. 18 Jun 2025) projects expected
  enforcement-demand from that date's weekday × hour pattern — **never measured
  congestion**. Lens shape: `{mode:'all'|'date'|'range', date, hour, start, end}`.

## Components (`src/components/`)

| Component | Role |
|------|------|
| `Header.jsx` | KPIs, search, sync/last-sync, About, Judge Tour, On-Duty toggle |
| `KpiStrip.jsx` | headline KPIs + quick-filters (P1, chronic, blind-spot, emerging, rising) + live-ops counts |
| `LiveMap.jsx` | leaflet command map; Simple-view toggle, hour-of-day slider, replay timeline, click-to-complaint (captures **vehicle number**, shown as a live tag + nearest station on the point) |
| `PriorityTable.jsx` | ranked queue (shows operational priority when live) |
| `FlowImpactView.jsx` | Carriageway Impact lens — ranked by modeled flow-impact proxy; highlights movers vs strategic rank |
| `TodayBoard.jsx` | live weekday+hour-aware emergency board (expected activity = priority + forecast + day/hour pattern + live reports); auto-refreshes, dispatch inline. **Expected enforcement-demand, not congestion.** |
| `OffendersView.jsx` | repeat-vehicle tracing — searchable vehicle list + per-vehicle time-wise log, top zones, mini-map. Anonymized IDs only. |
| `TimeLensBar.jsx` | **global** date/prediction control — **pick any calendar date** (Today / Tomorrow / Pick date / Date range) lifted to `App`; re-weights map, queues, flow-impact, staffing. In-window date = recorded; future date = projected via that date's weekday × hour pattern. |
| `OfficerDemand.jsx` | Staffing view — expected load → officers **needed vs deployed-now (live dispatches) vs station force**, per nearest station; tunable heuristic, not congestion. |
| `OperationsConsole.jsx` | the live closed-loop console (complaints/dispatches) |
| `ZoneDrawer.jsx` | per-zone detail; bias raw→adjusted rank story; shows the 3 numbers |
| `TimingGap.jsx` | enforcement-timing gap + evening blind spots |
| `CoverageSimulator.jsx` | coverage/ROI curve, top-K deployment sim |
| `ForecastView.jsx` | next-month forecast |
| `TypologyView.jsx` | zone clusters |
| `StationView.jsx` | per-station command summary |
| `ValidationPanel.jsx` | methodology + sensitivity/backtest; **real** client-side re-rank slider |
| `OfficerView.jsx` | mobile officer UX: jobs-now, reports-near-you, report-a-problem, plain forecast |
| `Dispatch.jsx` | standalone dispatch status screen |
| `WhatNow.jsx` | "what to do now" prompt (hidden during hour/replay focus) |
| `AboutModal.jsx` | PS1 problem-statement mapping |
| `JudgeTour.jsx` | 7-step guided tour |

## Libraries (`src/lib/`)

- `api.js` — API + offline fallback (above). All network access goes through here.
- `timeLens.js` — global time/prediction lens logic: window weighting
  (`zoneActivity`, `activityField`, `adjustedScore`) + officer-demand heuristic
  (`areaExpectedTickets`, `officersNeeded`). Honesty labels live here.
- `localOps.js` — offline mirror of the backend operational rules.
- `plain.js` — **plain-language translation layer** for the officer view. Maps
  tier→"Top priority", pressure→"blocks traffic constantly", responsiveness/
  habitual/blind-spot → plain sentences, plus km/ago/haversine helpers. **No raw
  scores or jargon are shown on the officer view** — route them through `plain.js`.
- `format.js` — number/label formatting helpers.

## Honesty rules in the UI

- Never label anything as "congestion" measured from data. Evening gap = coverage
  gap vs *assumed* peaks; hour-slider activity = **recorded enforcement activity,
  not traffic** (label it as such — e.g. 10:00 = many zones active, 19:00 ≈ empty).
- In the zone drawer / operations views, keep the three numbers visibly distinct:
  historical priority (immutable) vs live adjustment vs operational priority.
- The validation slider is a real client-side re-rank from the A/B/C pillar
  scores, labelled exploratory, resettable to 50/30/20. Keep it real — do not
  reintroduce a fake/dead control.

## Conventions for new work

- Keep the gzip bundle lean (~120KB). Avoid heavy deps.
- Any new data dependency needs the full chain: pipeline emit → demo bundle →
  backend route → `api.js` fallback → component.
- Mind React hooks order (a past bug was a conditional-hook ordering issue in
  `CoverageSimulator`); declare all hooks before any early return, as `App.jsx` does.
- Match the existing terse JSX style and the single `app.css` stylesheet.
