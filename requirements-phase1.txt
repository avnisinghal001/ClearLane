# ClearLane Phase 1 only: data foundation, profiling, quarantine, reports, tests.
# Keep this separate from api/requirements.txt so Vercel serverless stays light.
pandas>=2.2
numpy>=1.26
pyarrow>=15
pydantic>=2.0
PyYAML>=6.0
pytest>=8.0
matplotlib>=3.8
