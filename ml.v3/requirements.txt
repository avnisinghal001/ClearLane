# ClearLane v3 — ML pipeline (Phases 1-8). Python 3.11+.
# Install:  pip install -r ml.v3/requirements.txt   then:  python ml.v3/run_all.py
pandas>=2.2
numpy>=1.26
scipy>=1.12
pyarrow>=15
scikit-learn>=1.4          # spatial-block CV (PoissonRegressor, GroupKFold) + HistGBR fallback
statsmodels>=0.14          # NB GLM w/ log-exposure offset (04) + fixed-effects causal panel (10)
h3>=4.1                    # hexagonal binning (02); v3 + v4 APIs both handled by utils
esda>=2.5                  # Getis-Ord Gi* + Moran's I significance (04)
libpysal>=4.9              # spatial weights for esda
lightgbm>=4.0              # daily Poisson day-of-week forecaster (06)
pulp>=2.8                  # MCLP exact dispatch-coverage solver (08)
pymongo>=4.8               # two-tier cache + serving (ml.v3/cache)
dnspython>=2.6             # mongodb+srv:// URIs

# Notes:
#  - Online drift (stage 09) uses a built-in Page-Hinkley / z-score test — `river` NOT required.
#  - catboost / shap are only used by the older ml/pipeline (v1), not by ml.v3.
